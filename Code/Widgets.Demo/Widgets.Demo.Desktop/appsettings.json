{
  "PromptConfig": {
    "JsonPrompt": "Create an Adaptive Card using valid Schema for a visually interesting Windows 11 Widget with emoji with a single Submit action called Refresh that uses Databinding with a dollar sign for fields using only fields from this API endpoint {0} that returns this data ```json {1}``` Do not return any non-JSON text or numbering"
  }
}